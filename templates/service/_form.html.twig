{{ form_start(form) }}
<div class="form-group">
    {{ form_row(form.nom) }}
    {{ form_row(form.slug) }}
    {{ form_row(form.duree) }}
    {{ form_row(form.actif) }}
</div>

<button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
